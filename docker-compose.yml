services:
  dartdeck:
    build: .
    container_name: dartdeck
    ports:
      - "8788:8000"
    volumes:
      - dartdeck_data:/data
    environment:
      - DARTDECK_DB=/data/dartdeck.db
    restart: unless-stopped

volumes:
  dartdeck_data:
